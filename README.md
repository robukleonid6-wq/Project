Простой ВЕБ сервер
Веб-сервер на Python с использованием сокетов.
Поддерживает GET/POST запросы и обслуживание статических файлов.

# Быстрый старт

python server.py
# Откройте в браузере
# http://localhost:8080
 Структура проекта
text
simple-server/
├── server.py          # Главный файл сервера
├── requirements.txt   # Зависимости (пустой)
├── README.md         # Эта документация
└── static/           # Статические файлы
    └── index.html    # HTML страницы
📋 Возможности
GET/POST запросы
Статические файлы (HTML, CSS, JS, изображения)
Простое API
Формы
Многопоточность

# Запуск

# Базовый запуск
python server.py

# Другой порт
python server.py --port 8000

# Доступ из сети
python server.py --host 0.0.0.0

# Другая папка статики
python server.py --static public

## Основные пути
/ - Главная страница
/static/index.html - HTML файлы
/form - Тестовая форма
/api/status - Статус сервера (JSON)
/static/style.css - CSS файлы
/static/script.js - JS файлы

# Использование
Положите HTML/CSS/JS файлы в папку static/

Они будут доступны по /static/имя_файла

Используйте /form для тестирования POST запросов

API доступно по /api/status и /api/echo

# Ошибки
Порт занят: Используйте другой порт --port 9000
Файлы не видны: Проверьте путь /static/имя_файла
Не запускается: Убедитесь Python 3.6+ и нет синтаксических ошибок
